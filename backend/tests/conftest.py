"""Shared pytest fixtures.

Placeholder for the skeleton. Later this hosts the FastAPI test client, a
transactional Postgres session, a fake ARQ queue, and ML mocks
(MockTranscriber / MockEmbedder / MockDiarizer) injected via dependency
overrides (ADR-010, CLAUDE.md testing philosophy).
"""
